# Architecture

> Auto-generated by /map on 2026-03-24

## Overview

A Discord Music Bot using Lavalink, built with Python and discord.py. It connects to a Lavalink server to stream music from various sources, features an interactive UI system for player controls, uses an AI brain (Gemini) to generate natural language responses for commands, and tracks listening history.

```
┌─────────────────────────────────────────┐
│              discord.py (bot.py)        │
├─────────────────────────────────────────┤
│         Commands & AI Brain Layer       │
├─────────────────────────────────────────┤
│            Wavelink (Lavalink)          │
└─────────────────────────────────────────┘
```

## Components

### Bot Core
- **Purpose:** Main entry point connecting to Discord and handling music commands (play, skip, pause, resume, etc.)
- **Location:** `bot.py`
- **Dependencies:** discord.py, wavelink, dotenv, utils.ai_brain, ui.views

### UI Manager
- **Purpose:** Handles interactive UI components in Discord (buttons for music controls) and manages message cleanup.
- **Location:** `ui/views.py`
- **Dependencies:** discord.py, wavelink

### AI Brain
- **Purpose:** Uses Google Gemini API to generate context-aware, natural language responses to bot commands in a specific persona.
- **Location:** `utils/ai_brain.py`
- **Dependencies:** google-genai, os

### History Logger
- **Purpose:** Tracks listening history and logs events.
- **Location:** `utils/listening_history.py`, `utils/logger.py`
- **Dependencies:** sqlite3 (implied or used internally)

## Data Flow

1. User sends a command or clicks a UI button in Discord.
2. `bot.py` or `views.py` receives the interaction.
3. The command interacts with `wavelink` to control the Lavalink music player.
4. `ai_brain.py` is called to generate a localized custom response using Gemini.
5. The UI is updated via `ui_manager.update_all_ui`.

## Integration Points

| Service | Type | Purpose |
|---------|------|---------|
| Discord | API | Chat interface and voice integration |
| Lavalink | Audio Server | Music streaming and playback |
| Gemini API | API | Generating natural language responses |

## Technical Debt

- [ ] Ensure database or file paths for `listening_history` and `logger` are robust (no explicit DB connection found in core, using local files or SQLite).

## Conventions

**Naming:** Python snake_case for functions and variables, PascalCase for classes.
**Structure:** Modular design separating UI (`ui/`), utilities/ai (`utils/`), and adapters (`adapters/`).
**Testing:** No test directories found.
